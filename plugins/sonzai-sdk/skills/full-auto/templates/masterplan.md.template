# Masterplan: ${PROJECT_NAME}

**Date:** ${DATE}
**Mode:** ${MODE}  <!-- greenfield | brownfield -->
**Verified at:** ${VERIFIED_AT}  <!-- date of last version-search -->
**Status:** awaiting approval

---

## 1. Client goals

${CLIENT_GOALS}

## 2. Sonzai integration

| # | Answer | Value | Rationale |
|---|---|---|---|
| 1 | Archetype | ${WIZARD_ARCHETYPE} | ${WIZARD_ARCHETYPE_RATIONALE} |
| 2 | Integration path | ${WIZARD_INTEGRATION_PATH} | ${WIZARD_INTEGRATION_RATIONALE} |
| 3 | Runtime mode | ${WIZARD_RUNTIME_MODE} | ${WIZARD_RUNTIME_RATIONALE} |
| 4 | Capabilities | ${WIZARD_CAPABILITIES} | ${WIZARD_CAPABILITIES_RATIONALE} |
| 5 | Brand / persona | ${WIZARD_BRAND_PERSONA} | ${WIZARD_BRAND_RATIONALE} |
| 6 | Proactive | ${WIZARD_PROACTIVE} | ${WIZARD_PROACTIVE_RATIONALE} |
| 7 | Scope (surfaces) | ${WIZARD_SCOPE} | ${WIZARD_SCOPE_RATIONALE} |
| 8 | BYOK posture | ${WIZARD_BYOK} | ${WIZARD_BYOK_RATIONALE} |

## 3. Tech stack

(${MODE}: ${STACK_SOURCE_NOTE})  <!-- "interactive intake" or "audit + confirmation" -->

| Layer | Choice | Version | Verified |
|---|---|---|---|
| Backend language | ${STACK_BACKEND_LANG} | — | ${VERIFIED_AT} |
| Backend framework | ${STACK_BACKEND_FRAMEWORK} | v${STACK_BACKEND_FRAMEWORK_VERSION} | ${VERIFIED_AT} |
| Frontend | ${STACK_FRONTEND} | v${STACK_FRONTEND_VERSION} | ${VERIFIED_AT} |
| Database | ${STACK_DB} | ${STACK_DB_IMAGE_TAG} | ${VERIFIED_AT} |
| ORM / DB layer | ${STACK_ORM} | v${STACK_ORM_VERSION} | ${VERIFIED_AT} |
| Auth | ${STACK_AUTH} | v${STACK_AUTH_VERSION} | ${VERIFIED_AT} |
| Production target hint | ${STACK_DEPLOY_TARGET} | — | — |

## 4. Architecture

${ARCH_NARRATIVE}

<!-- Optionally a text or mermaid diagram below -->

${ARCH_DIAGRAM}

## 5. File structure

${FILE_STRUCTURE_TREE}

## 6. docker-compose plan

```yaml
${COMPOSE_PLAN}
```

Env vars expected (`.env.example`):

```
${ENV_VARS}
```

Ports:

```
${PORTS}
```

## 7. Scope

**In:**

${SCOPE_IN}

**Out:**

${SCOPE_OUT}

## 8. Risks & open questions

${RISKS_AND_OPEN_QUESTIONS}

## 9. CTO review notes

<!-- Operator: write any review notes here before approving. Free-text. -->

${CTO_REVIEW_NOTES}

---

## Approval

Operator: mark one and reply `approve` / `edit` / `reject <txt>` in chat.

- [ ] approved
- [ ] edited above and approved
- [ ] rejected

---

*Generated by `cto-loop` Gate A on ${DATE}. Versions verified ${VERIFIED_AT}.*
